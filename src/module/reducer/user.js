const defaultState = {};

const userReducer = (state = defaultState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export const namespace = 'user';

export default userReducer;
